---
- hosts: router CSR1
  gather_facts: false
  connection: local
  tasks:
  - name: show run 
    ios_command:
      commands:
      - sh run 
    register: shrun
  - name: report informaiton 
    debug:
      var: shrun 